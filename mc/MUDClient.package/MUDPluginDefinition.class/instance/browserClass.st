accessing
browserClass
	"Answer browser (a child of RpcBrowser) used for displaying the plugin.
	 If position = none, this method should return a class (it will instantiate it to create the
	 window plugin."
	^ nil